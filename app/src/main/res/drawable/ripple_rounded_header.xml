<?xml version="1.0" encoding="utf-8"?>
<!-- Ripple background for collapsible-section headers (the Apps row
     in Channel Input and the Devices row in Audio Output). The
     default `?attr/selectableItemBackground` gives a sharp-edge
     full-bounds ripple; we clip it to 12dp rounded corners via the
     mask item so taps look at home next to the rounded section cards
     below the header. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</ripple>
